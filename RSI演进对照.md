# RSI 演进对照 — evolver.py × arXiv:2609.11873

> 2026-09-16。依《The Last AI Built by Humans: Toward Genuine Recursive Self-Improvement》
> （SJTU / Theseus Labs / 清华 / 字节，33 作者，2026-09-10，75 页）之五级自主框架，
> 对照 evolver.py 当前实现，给出突破性改进方案。
> 与《演进方案.md》的关系：彼为现行稳定化章程（soak 纪律不破）；本文为 soak 后的
> 技术路线输入，P0 项可在 soak 期内以「纯测量 / 带外安全治理」形式先行。
>
> **实施状态（2026-09-17 全项目审阅后更新）**：P0 两项已落地并经生产实证——
> P0-1 锚定评测（`gep/anchor.py` + **epoch 4 × 9 冻结探针** + `anchor_failed`
> 硬失败 + CLI）：**连续六次生产触发全绿**（round-16/19/20/21 验证面变异 ×4
> 于 epoch 3 全 8/8；round-23 于 epoch 4 首产 9/9——第 9 探针即遥测不变量，
> 米尺自此同受锚治理），负向验证通过；P0-2 meta-report（`ops/meta_report.py`
> + CLI）：六维面板 + 后代质量 + structural-L5 审计（10 行）+ ms/gain +
> timing_coverage，口径经三轮诚实性修复（#29 transfer 差分头集、#30 分子分母
> 同种群+失败成本入账、#31 单调钟）。
> **soak 已达标并出判定：gated 20/20，verdict=`false_kill_high`**（唯一拦截是
> T0 分块超时伪杀——门自身测量如实报告了「过紧」，转正不切；round-22 已落地
> 噪声免疫校准，干净样本累积中）。P1-3/4/5、P2-6..10 未动（grep 确认）。
> 落地细节、诚实缺口与下一步见 **§五 落地实况审计**；effective-L5 首次对照
> 与复跑协议见 **§六**。

---

## 一、论文核心拆解

### 1.1 基本单位：改进环（improvement loop）而非算法

论文拒绝以「用了什么算法」定义 RSI，改以九组件环为分析单位：

| 组件 | 定义 |
|---|---|
| AI system | 被跨轮追踪能力的完整计算实体（含机制与持久态） |
| System state | 一轮结束被继承到下轮的部分 |
| Experience | 早期交互获得、用于指导后续修改的信息 |
| Target | 本轮被直接修改的对象（后来也可成为改进机制本身） |
| Improver | 把当前状态+经验变换为候选修改的机制 |
| Strategy | Improver 决定「往哪搜、怎么生成候选」的方法（可被保留→可成为 Target） |
| Verifier | 评估候选并执行接受规则的机制 |
| Improvement | 通过当前接受规则、被保留、进入下一状态的变更 |
| Successor | 继承了已接受改进、带着更新状态进入下一轮的系统 |

三问贯穿全文：**环在哪闭合？什么被更新并继承？哪些决策仍在外部？**

RSI 定义（§2.2.2）：系统通过持续交互，**自主**把经验与反馈转化为对自身的**持久**变更，
且这些变更**能进一步影响后续改进的生成、评估、选择与巩固机制**——「改进能力本身
成为递归过程的一部分」。

### 1.2 五级自主框架（§3）与晋级边界（§3.7）

| 级 | 名称 | 边界判据（一句） |
|---|---|---|
| B0 | 环内改进 | 仅精炼当前任务输出，不留存 |
| L1 | 改进执行自主 | **持久性**：已接受变更存活过本任务；人定目标/程序/验收，AI 执行 |
| L2 | 改进策略自主 | **策略选择转移**：AI 依评估反馈决定「下一次试哪个干预」而非仅执行处方 |
| L3 | 经验获取自主 | **学习议程**：演化中的学习者影响「接下来获取什么经验」并回流 |
| L4 | 环境适应自主 | **部署适应**：运行期交互证据决定哪些持久变更在变化条件下保持有用 |
| L5 | 递归元改进 | **递归继承**：支配后续改进的机制本身成为被继承的改进对象 |

关键细分：**structural L5**（改动持久且实际支配了后续某轮改进）vs **effective L5**
（在同等预算与独立评测下，修订后的机制产出/选出更好的后继）。论文明确：现有系统
全部只到 structural；effective L5 是实质性经验空白。

### 1.3 HCI 与领域现状

Headroom-Closed Index：基准分对「入年 90 分位前沿=0、满分=100」归一。2026 年：
高等数学 86.4、科研级科学 85.8，而**软件工程仅 52.6、工具智能体 39.9**——evolver
所自动化的恰是余量最大的域，项目选题被数据背书。

### 1.4 S3 软工场景现状盘点（§4.3，与本项目同域）

论文结论：L1 最普及（经验库/技能库），**L2 是已确立的最强级**（harness 搜索：
Self-Harness、Agentic Harness Engineering、Ouroboros；种群：DGM、DarwinX、
Group-Evolving Agents；元选择：HGM），L3 起步（SWE-RL / Socratic-SWE 自博弈任务
生成），**L4 在软工领域基本缺席**，L5 有界（SICA/DGM 的 structural 特征），
**完整 L5 尚未实现**。

### 1.5 八大挑战（§6，压缩）

1. 跨组件诊断：失败不指认应改的组件；需可检验干预假设 + 受控对照 + 组件冻结。
2. 学习者条件化经验获取：正确性≠难度≠学习价值；经验腐蚀会跨轮传播。
3. 持久态管理与可靠复用：**Library Drift**——技能库膨胀反噬检索；需准入→激活→
   忠实使用→复检→退役全生命周期，以及适用条件/依赖/后效元数据。
4. 领域反馈下的受治适应：区分瞬态失败与复现局限；发布前验证；人类保留权威。
5. 改进机制的可信演化：**评测者自偏好**是结构性风险——RQGM 的答案是纪元内冻结
   评测者 + 独立锚 + 更换时重评受影响分数。
6. 继承改进能力的长程评测：六维——adaptivity / retention / transfer / efficiency /
   stability / **meta-recursion**；须报完整轨迹（含被拒、回退、恢复）。
7. 资源感知与人类协作：以「每单位验证收益的成本」计账；自适应停止。
8. 跨轮继承的可复现基建：统一工件格式连接 父状态→变更→动机证据→评测配置→
   接受决定→后续使用。

---

## 二、evolver.py 逐级定位

### 2.1 环解剖映射（论文组件 ↔ 本仓实现）

| 论文组件 | evolver 对应 | 所在 |
|---|---|---|
| AI system | 引擎 + MCP 宿主执行器（引擎无 LLM 依赖，宿主即 improver 之躯） | swarm.py / mcp_server.py |
| System state | git 提交链 + genes/capsules.jsonl + LESSONS_LEARNED + memory_graph | .evolver/gep/, memory/evolution/ |
| Experience | signals（摩擦/错误/preflight_abort/feedback E） | signals.py, pending_signals |
| Target | 引擎源码本身（含选择器、变异引擎、验收门=改进机制可成靶） | 全仓 |
| Improver | 宿主 Agent 执行 GEP dispatch 提示词 | 蜂群接管协议 |
| Strategy | selector 评分（signals_match+表观遗传+冷却+惰性禁用）+ adaptive 偏置 | selector.py / adaptive.py |
| Verifier | ruff→mypy→pytest 级联 + T0 冻结验收门（shadow soak）+ fitness 门 | solidify.py / acceptance/ |
| Improvement | 通过级联与门的固化提交 + 基因入库 + 活记忆 | solidify.py |
| Successor | 下一 tick 的引擎（新提交 + 新基因库） | 闭环本身 |

三问作答：**环闭合于仓库自身**（dogfood 即版本化继承）；**被继承的是引擎代码 +
基因库 + 活记忆**；**外部保留的是** HITL 审批、HOTL 否决/暂停、验收门转正权、
（人写的）选择器启发式本身。

### 2.2 级别判定

| 级 | 判定 | 证据 |
|---|---|---|
| L1 | **巩固达成** | 固化提交/基因/活记忆跨轮存活；21 轮 dogfood、20 gated run、版本钉在 1.112.0（章程纪律） |
| L2 | **芽** | adaptive.py 依反馈 E 移策略权重、降级连击→repair 枢转、平台期→novelty——「证据改变下轮试什么」已存在；round-19→20→21 展示了遥测→摩擦→下轮变异的完整证据链（宿主读面板选干预），但选择器启发式+宿主仍是决策者，每轮**单候选贪心**，无搜索；P1-4 把这条链机械化后才算晋级 |
| L3 | **消费级** | autopoiesis 摩擦→信号、question_generator（悬赏问题）、curriculum.py（flag 默认关）皆是「失败回声」驱动的消费者；无自主生成/寻求经验的议程 |
| L4 | **域内特化、超同期软工系统** | 论文称 L4 在软工「基本缺席」，而 evolver 的部署场即进化场（引擎边跑边进化），且回归感知选择（T0 冻结分母）+ 版本化继承俱全；短板：单工作区单宿主，无跨环境适应 |
| L5 | **structural 有测量、effective 首读阳性但未证** | structural-L5 审计行已运行（10 行，后代成功率 60–100%）；effective 受控对照已跑首次（§六）：解锁性真阳性判据满足（eval worktree 首产即逮 #33）、锚/T0 稳定不降，但 N=6 且混淆不可剥离——仪器与首次读数就位，不宣称等级 |

### 2.3 论文对现有设计的背书（勿动的部分）

- HITL/HOTL = 论文 L5 节「人类保留否决与部署权威；固定基建执行之，无需逐轮审批」
  的精确实现；AUTO_HIJACK=1 强制开门与 fail-closed 完全同构。
- events.jsonl 只追加、双 id 谱系（gene_id 剧本 / landed_gene_id 落地）、证据目录
  （头尾双留）= 挑战 8 工件格式的雏形。
- T0 冻结集 = 「回归感知选择」的分母稳定契约（soak round-3 修的正是论文点名 的
  「重新冻结令删除测试从分母消失」）。
- 验收门方法论出处即论文所引 Self-Harness（arXiv:2606.09498，代码内已注明）。
- dogfood 的「开发挑战→代理级修改→仓库接地试验→回归感知选择→版本化继承」
  与论文 S3 理想环逐项对位。

---

## 三、突破性改进点

> 排序原则：P0 = soak 期内可做（纯测量/带外治理，不违章程「不加旋钮、不切
> SHADOW」）；P1 = soak 转正后第一波机制升级；P2 = 结构性扩展。
> 每项含：论文依据 / 现状差距 / 方案 / 接入点 / 验证 / 风险。

### P0-1 锚定评测（Anchor Eval）——封死评测者自偏好

- **依据**：挑战 5 + RQGM（§3.6.2）——「对评测者反复施压会筛选出利用其盲区的
  提案」；纪元内冻结 + 独立锚 + 更换时重评。
- **差距**：验收门（acceptance/）是**可被变异编辑的仓内代码**，其守护者是级联
  pytest——而 pytest 测试也是仓内可编辑物。一个同时弱化门与其测试的变异，会被
  被弱化的门接受：**自偏好回路在结构上开着**。目前靠「人的审查」这道带外闸
  兜底（DEBUG.md 纪律），无代码化防线。
- **方案**：
  1. `$EVOLVER_HOME/anchor/`（仓外、gitignore 不可达）存放冻结锚任务集：一组
     版本钉死的「引擎能力探针」——如固定 commit 的微型工作区 + 期望的
     tick→solidify 行为契约、一组已知缺陷的复现用例（取自 DEBUG.md #1–26 的
     可执行化）、以及当前 T0 种群的一次性快照通过率。
  2. 触发规则：变异 diff 触碰 `src/evolver/gep/acceptance/`、`solidify.py` 的
     Verifier 段、或 `tests/gep/acceptance/` 时，级联追加锚运行；锚失败 =
     hard failure（不可 retryable）。
  3. 纪元纪律：锚任务集只允许人手工更换（更换即新纪元，记录于锚目录
     `epoch.json`），引擎永不写锚目录（路径权限与 `~/.evomap` 同级）。
- **接入点**：`solidify.py::_run_validations` 级联命令组装处按 diff 触发；
  `gep/paths.py` 加 `get_anchor_dir()`；DEBUG.md 历史条目逐条可执行化为锚用例
  （一次性的批量工程，可作 dogfood 轮次内容）。
- **验证**：构造「弱化门的变异」实验（临时调宽 acceptance 阈值）必须被锚拦下，
  而仅靠仓内测试拦不下（负向验证）。
- **风险**：锚集腐化（引擎演化后旧锚语义失效）——纪元更换机制正是为此；
  锚运行成本——探针为微型契约，分钟级。
- **为什么是 P0**：验收门转正（SHADOW=0）的前置条件就包括「门自身不可被
  循环论证污染」；这不是新能力，是把已有纪律（人审）代码化。

### P0-2 有效 L5 遥测——从 structural 走向 effective 的第一步是测量

- **依据**：§3.6.5 Table 8 六维 + HGM 的核心论点：「当前性能 ≠ 产出更好后代的
  能力（metaproductivity）」；挑战 6 要求报完整轨迹。
- **差距**：feedback.jsonl 与 events.jsonl 已含全部原料，但从未回答「第 N 轮
  接受的变更是否提高了后续轮次的改进速率/质量」。soak 报告只看门的
  interception/false-kill，不看**改进机制自身的效率轨迹**。
- **方案**：新增 `evolver meta-report`（CLI + gate-report 并列输出，纯读）：
  1. **改进速率**：滚动窗口内「已验证收益 / 轮次」与「/ 成本（级联秒数、
     token 不可得则次数）」——收益=通过门的变异数、被拒数、回滚数全轨迹。
  2. **后代质量**：按 landed_gene_id 聚合——其落地后 K 轮内，相关信号族的
     解决率、复发率（memory_graph 已有 outcome 记账，直接复用）。
  3. **机制审计行**：凡变异触及 selector/adaptive/acceptance/prompt 装配
     （=改进机制面），标注 structural-L5 事件，附「修订动机证据（摩擦信号）
     + 后续是否被调用（后续轮选择/门运行引用）」——events 双 id 谱系
     已备，只差聚合视图。
  4. 六维面板：adaptivity / retention / transfer（跨信号族）/ efficiency /
     stability（回滚与退化反馈计数）/ meta-recursion（机制复用率）。
- **接入点**：新 `ops/meta_report.py`（只读聚合，无写入路径、无新 env 旋钮）；
  gate-report 增补引用。
- **验证**：对历史 15 轮数据回放，产出首份《改进机制效率轨迹》——这本身就是
  dogfood 的下一份高价值产出。
- **风险**：归因混淆（收益归因到哪轮变更）——先用粗粒度滚动窗，不追求精因果。

### P1-3 候选种群与谱系档案——单候选贪心 → 种群搜索

- **依据**：S3 §4.3.3——DGM（变体档案+亲代选择，防单害 Edit 锁死谱系、暂弱
  变体可成垫脚石）、DarwinX（**仅当扩展任务覆盖且不回退既有解时准入**）、
  Group-Evolving（跨谱系共享成功与失败经验）。
- **差距**：每轮一个基因→一个变异→接受/拒绝，单贪心谱系；被拒变异的 diff
  虽存档（novelty 指纹）但**不进入候选池**；无「暂弱但后代强」的保留通道。
- **方案**：
  1. 每轮派发 K 个候选（K 起步=2），经 `EVOLVER_FF_ENABLE_EVAL_WORKTREE`
     已有的 worktree 桥（S26.5）并行隔离试验；
  2. 准入规则复用 T0 冻结集语义：候选须 ≥ 基线 − ε 且不打破基线已解子集
     （DarwinX 语义天然内建于现有 T0）；
  3. `candidates.jsonl` 升格为变体档案：含 diff、结果、亲代 run_id、
     「被拒但保留」标记——后续轮可将档案条目作为 recall 候选重派（DGM 语义）；
  4. 准入与否与 cooldown/inert-ban 联动，防档案膨胀成 Library Drift（见 P1-5）。
- **接入点**：dispatch 阶段多候选参数化（不改 env，用既有 feature flag 机制）；
  `gep/bridge.py` worktree 并行；selector 的 `_applied_cooldown_ids` 复用为档案
  淘汰输入。
- **验证**：soak 后小规模试点（K=2）对比 K=1 的六维面板差异；要求回放中
  「被拒→重派→接受」案例至少一例。
- **风险**：成本翻倍（K 倍级联）——按挑战 7 资源计账，先只在摩擦信号
  高优先级轮启用。

### P1-4 证据包派发——L2 策略选择权从启发式移交给证据

- **依据**：L2 定义（「把观测证据变成下一个实验」）+ GEPA 的轨迹反思 +
  SWE-Exp 经验银行（成功与失败轨迹皆入库、按仓库任务检索）。
- **差距**：dispatch 提示词已有 Recall Hints（成功侧），但**没有失败侧证据包**：
  本信号族过去试过什么、被拒原因、其后代表现；宿主拿到基因剧本但没有
  「为什么是这个干预」的证据链，选择权实际在固定启发式手里。
- **方案**：
  1. dispatch 组装「证据包」：按当前 signals 聚合 events.jsonl 近 N 条
     （同族）——已试干预及其结局、novelty 指纹（防重复）、DEBUG 摩擦规则
     （autopoiesis_rules 已有）、P0-2 的后代质量行；
  2. 提示词第三章增设「干预提议」节：宿主可 (a) 复用基因，(b) 提出新颖
     策略进 proposal 通道（S29 机械提案已在演进方案 §7 排期——两事天然
     合流），引擎端 selector 降格为「检索增强 + 约束检查」而非唯一决策者；
  3. 提议仍过同一 Verifier（级联+门+锚）——策略自主扩权、验收权不动，
     与论文 L2 边界（「无权重定义何为成功」）严格一致。
- **接入点**：`evolve/pipeline/dispatch.py` + `gep/prompt.py` 装配；
  `gep/proposal.py`（已存在）。
- **验证**：A/B 对比——证据包开/关的干预命中率（被接受率）、重复干预率下降。
- **风险**：提示词膨胀——证据包预算化（复用 `SWARM_TICK_LOG_MAX_CHARS` 思路，
  但不加新旋钮，硬编码上限）。

### P1-5 基因全生命周期治理——Library Drift 防治

- **依据**：挑战 3——库膨胀反噬检索（Library Drift）；需准入→激活→忠实使用→
  复检→退役，及适用条件/依赖/后效元数据；「检索到≠被遵循」两态分离。
- **差距**：基因库 35 条且只增不减；signals_match 是词面匹配，无适用条件
  元数据；asset_call_log 有复用归因而无「忠实使用」判定；惰性禁用与冷却
  是仅有的退化机制，无复检无退役。
- **方案**：
  1. Gene schema 增 `applicability`（信号族+前置条件机读）与 `dependencies`
     （PYDANTIC 字段加可选键，向后兼容）；
  2. 生命周期状态机：`active → under_review → retired`，触发器来自 P0-2 的
     后代质量（持续零工作结果/负迁移）与检索命中未遵循率；
  3. 复检：retired 前给一次「重验窗口」（在锚或 T0 子集上重跑其 validation）；
  4. 检索质量面板进 meta-report（激活率/忠实率/后效）。
- **接入点**：`gep/schemas`（Gene 模型）、`selector.py`（状态过滤）、
  `asset_call_log.py`（忠实使用判定钩子）。
- **验证**：人工注入一条「高频命中但零后效」基因，应在 N 轮内走完
  under_review→retired。
- **风险**：误退役——重验窗口 + 人审保留（HITL 已有骨架）。

### P2-6 自博弈挑战生成——L3 学习议程

- **依据**：L3 定义（学习者条件化的**未来经验获取**）+ Socratic-SWE / SWE-RL
  （生成针对能力缺口的可验证任务）+ 挑战 2（正确性≠难度≠学习价值）。
- **差距**：curriculum.py 消费失败回声，不生成挑战；引擎的「能力缺口」
  （锚未覆盖的失败模式、级联拒因聚类、DEBUG 新条目）无人系统化盘点。
- **方案**：
  1. **能力缺口账本**：聚合 T0 子集失败签名、级联拒因、`_classify_failure_mode`
     输出、DEBUG.md 新条目，形成缺口条目（信号族+复现方式+难度估计）；
  2. **挑战生成**：在一次性 scratch 仓（非活仓）注入可验证缺陷——变异类型取自
     缺口账本（如「状态文件竞争」「PATH 缺失」「谱系错位」等本仓真实踩过的坑
     的泛化）；question_generator 的悬赏机制作为优先级排序器复用；
  3. 练习闭环：挑战→宿主修→scratch 仓级联验证→蒸馏基因（挑战即 dispatch 的
     特殊形态，走既有 Verifier）；
  4. 学生-条件化：缺口账本按「当前引擎实测失败率」排序（不是静态清单），
     挑战难度跟随能力（L3 判据）。
- **接入点**：`gep/curriculum.py`（已有 level 机制）、`gep/question_generator.py`、
  worktree 桥。
- **验证**：生成挑战的「可验证率」（级联能判）与「学习价值」（修过挑战后
  同族真实摩擦复发率下降）。
- **风险**：合成任务分布偏移（论文挑战 2 点名）——缺口账本必须锚定真实失败
  签名，不从模板空想。

### P2-7 跨组件诊断账本——把 DEBUG.md 方法论代码化

- **依据**：挑战 1——失败不指认应改组件；需可检验干预假设、受控对照、
  组件冻结、依赖记录。
- **差距**：DEBUG.md 四段式（症状/根因/修复/经验）本质就是论文要的「干预假设
  记录」，但它是人写的散文，机器不可聚合；failure_mode 三分类过浅。
- **方案**：诊断条目结构化：`{症状签名, 嫌疑组件列表, 假设, 对照实验(冻结X改Y),
  结论, 归因组件}` 入 JSONL；级联拒因自动开条目，宿主修完回填归因——
  供 P1-4 证据包与 P0-2 面板消费；DEBUG.md 保留为人类可读投影（生成式文档）。
- **验证**：同一签名复发时，诊断账本能检索出历史归因（现在要人翻 DEBUG.md）。

### P2-8 统一继承工件——挑战 8 的完整闭环

- **依据**：挑战 8 工件六元组：父状态→变更→动机证据→评测配置→接受决定→
  **后续使用**。
- **差距**：五元组散落在 events.jsonl/evidence/WAL/snapshots 且彼此引用靠约定；
  「后续使用」完全缺失（基因被派发≠被调用≠被遵循）。
- **方案**：以事件 id 为主键的物化视图（meta-report 生成，只读），补「后续
  使用」维度：landed_gene 的后续派发/命中/遵循/后效时间线。不迁旧数据，
  视图层聚合即可。

### P2-9 多节点种群共享——蜂群侧的 Group-Evolving

- **依据**：Group-Evolving Agents（跨谱系共享成功修改与失败经验）+ DarwinX
  种群准入。
- **差距**：mailbox/A2A/Hub 基建在，但节点间只传消息不传**进化素材**；
  单节点即单谱系。
- **方案**：Hub 资产同步通道（sync.py 已有）增加「失败经验」类型
  （novelty 指纹 + 拒因）的共享；接收侧准入走本地门（不信任远端判定，
  论文「保护性评测」纪律）。

### P2-10 soak 报告 v2 —— 引擎自己的 HCI 画像

- **依据**：HCI 归一化方法论 + 挑战 6 完整轨迹。
- **方案**：gate-report 增「能力轨迹」节：以 dogfood 起点为 0、以章程目标
  （20 gated、interception∈[0.05,0.5]、false-kill≤0.1）为 100，逐维归一画
  轨迹——把 P0-2 六维与 soak 三判据统一成一张「余量地图」，替代单数字
  verdict。人看图决策，比看 collecting 一词信息量大一个量级。

---

## 四、路线图与章程的关系

| 阶段 | 内容 | 与演进方案.md 的关系 |
|---|---|---|
| soak 期内（**已完结**） | P0-1 锚定评测、P0-2 meta-report | 已按纪律完成：零新 env 旋钮、SHADOW 未动、版本钉 1.112.0；soak 20/20 出判定 `false_kill_high`——**数据不支持转正**，校准后再议（§五） |
| 门校准期（现在） | T0 分块超时重试 + 重复间方差异议裁决，重攒 soak 样本 | 仍是纯测量修复；转正与否永远留人类 |
| soak 转正后第一波 | P1-3 种群、P1-4 证据包、P1-5 生命周期 | 与 §7 既定次序（S26.5 干净 worktree 门、S29 机械提案、S30 环境变量削减）合流：P1-3 用 S26.5 的桥、P1-4 用 S29 的提案通道 |
| 结构扩展 | P2-6..P2-10 | 下一份章程的候选清单，按 P0-2 的实测数据排优先级 |

**一句话总结**：论文判定「五级之中，软工域 L2 已确立、L4 缺席、L5 只有结构无实效」。
evolver 恰好倒挂——L1/L4 强（部署即进化场是同期软工系统没有的特化），L2/L3 弱
（单候选贪心、消费型经验），L5 有结构无测量。突破路径不是补弱项齐头并进，
而是**先以 P0 两项把「自己改进自己」的安全性与效率变成可测量的量**（锚防
自偏好、遥测防自欺），再以种群与证据包完成 L2 跃迁——这正与现行章程
「先稳互锁、再谈转正、后收割」的节拍器同频。

---

## 五、落地实况审计（2026-09-17 全项目审阅）

> 依 live 遥测逐点核对代码与账本后的诚实盘点：什么真的在生产运转、数字说了
> 什么、哪些缺口是本次审阅新发现的。

### 5.1 P0 两项的生产实证

**P0-1 锚定评测**（`gep/anchor.py`，epoch 3 × 8 探针，`$EVOLVER_HOME/anchor/`）：

- 触发接线真实工作：round-16/19/20/21 四次验证面变异（solidify.py×3、
  acceptance/orchestrator.py×1）全部触发锚，**4/4 轮 8/8 探针通过**并记
  `anchor_result` 于事件；
- 负向验证在先（弱化 dup-solidify 守卫→对应探针变红）；
- 纪律守住：epoch.json 只由人 CLI 写（`evolver anchor init --epoch`），
  引擎只读；epoch 1→2→3 严格递增（round-16 探针源码修 ruff/mypy 时重装两次）。

**P0-2 meta-report**（`ops/meta_report.py` + CLI `evolver meta-report [--json]`）：
live 读数（2026-09-17）：

| 维度 | 读数 | 解读 |
|---|---|---|
| adaptivity | 29 事件 / 22 接受 / 7 失败，窗口序列 [3,5,3,4,3,4] | 无恶化趋势，平台期是实况（0.9997 之上增益本就稀疏） |
| retention | 13 落地基因追踪 / 5 信号无复发 / 0 后代不足 | 复发-free 占比 38%——修过的摩擦多数没再犯 |
| transfer | **0**（差分头集口径） | 诚实零：尚无基因在剥离常驻背景后跨信号族复用（P1-3/4 的动机） |
| efficiency | 1.32 rounds/gain；3.27M ms/gain（timed 6/22） | 均值被 18.3M 睡眠污染事件主导（5 个干净样本 ≈235s/gain）——**覆盖率字段让污染可见**，这正是 #30 的价值 |
| stability | 7 失败 / 0 降级反馈 | 失败全部入账（#30 后失败也带 timing） |
| meta-recursion | structural-L5 变异 10 行 | 近三行 solidify.py 后代成功率 100%、orchestrator 行尚无后代（unknown） |

口径诚实性本身经三轮修复（#29 差分头集 / #30 同种群+失败成本 / #31 单调钟）
——**遥测的演化史就是「防自欺」的实战记录**。

### 5.2 soak 判定：数据不支持转正（这是系统的成功，不是失败）

`gate-report --json`：gated_runs **20/20**、shadow_rejected 1、
interception 0.05（踩 [0.05,0.5] 下限）、validation_disagreements 1、
**false_kill_risk 1.0 > 0.1 → verdict `false_kill_high`**。

唯一次拦截的解剖：round-21 的 T0 repeat0 测 0.8863、repeat1 0.9997——0.886
即 #25/#26「分块超时→整块归零」旧签名；同轮级联 pytest 全绿、变异只改
layer_id 字符串。**纯测量噪声伪杀**。false_kill_risk=1.0 把这件事如实摆在
台面上：soak 的设计目的（让伪杀在 shadow 里现形，而不是硬执法杀好变异）
达成。校准方向（下一 dogfood 弧线）：分块超时单块重试 + 重复间方差异议
裁决（repeat 间差 >ε 时追加仲裁轮取中位）。**转正（SHADOW=0）始终是
人类决策，且当前数据明确说不。**

### 5.3 本次审阅新发现的诚实缺口

1. **soak 在仓内进行**：`inside_repo=true`——20 个 gated run 累积在产品仓的
   `.evolver/`/`memory/`，未按演进方案 §10 处方用外置 soak 工作区
   （`~/.evomap/evolver.py-soak` 不存在）。缓解成立（运行态 gitignore、
   从未提交、锚在仓外），但「soak 状态与产品仓隔离」的处方未照办——
   重攒校准样本时应改用 `evolver soak setup`。
2. **S26.5 eval-worktree 旗标全程未开**：29/29 事件无 `eval_workspace`——
   级联与门都在活树测量（引擎按设计回退 live cwd）。门的候选树=活树，
   「干净 worktree 门」从未真正运行过。
3. **测量仪器自外于锚与机制面**：`ops/meta_report.py` 既不在
   `ANCHOR_TRIGGER_SURFACES` 也不在 `META_MECHANISM_SURFACES`——遥测自身的
   变异（round-18/19）不触发锚、不计 structural-L5。而连续三轮修的都是
   「表」的诚实性（#29/#30/#31）。**建议 epoch 4**：加遥测不变量探针
   （合成事件集上断言分子分母同种群、单调时长 ≤ 挂历跨度），并把
   meta_report.py 纳入机制面。
4. **effective L5 仍未测**：structural 有了数字（10 行、后代 60–100%），
   但论文标准的受控比较（同预算+独立评测下，修订后的机制是否产出更好
   后继）一次都没跑。仪器已备（T0 基线 + ms/gain + 锚），缺的是实验设计：
   候选=机制变异前后各 N 轮同信号族对照。这是 P1 之前最值得做的
   「零新代码」实验。**→ 2026-09-17 已跑首次对照并固化复跑协议，
   见 §六：方向性阳性（解锁性真阳性判据满足）、统计未证（N=6）、
   不宣称等级。**

### 5.4 章程纪律核对（全部守住）

版本钉 1.112.0（round-13~21 零 minor bump）✓；零新 `EVOLVER_*` 旋钮
（#29/#30/#31/#32 四次修复均无新 env）✓；SHADOW 未动 ✓；运行态零提交
（`git ls-files memory` 仅 LESSONS_LEARNED.md）✓；锚仓外+epoch 递增 ✓；
DEBUG.md 惯例不辍（#29–#32 四条四段式）✓。

### 5.5 结论与排序

P0 的落地把论文两个最尖锐的警告变成了生产行为：**评测者自偏好**（锚四产
全绿+负向验证）与**自欺式遥测**（六维面板+三轮口径修复+覆盖率暴露）。
soak 判定 `false_kill_high` 是测量系统在正确工作。下一步排序：

1. **门校准**（分块重试+方差异议裁决）→ ✅ round-22 已落地（DEBUG #33，
   eval worktree 同轮首产）；重攒 soak 继续用干净样本（外置工作区债
   留待 soak 重启时清）；
2. **effective-L5 对照实验** → ✅ 首次已跑+协议固化（§六）；复跑挂钟
   每 ~10 轮；
3. 锚 **epoch 4**（遥测不变量探针 + meta_report 纳入机制面）→ ✅
   round-23 落地（DEBUG #34；第 9 探针 telemetry-invariants，epoch 4
   首产 9/9）；
4. 失败事件补 `eval_workspace` 元数据（§6.5 观测盲点，可与 3 合并一轮）
   → ✅ round-23 同轮落地（三条拒绝路径，过滤与成功路径一致）；
5. 然后才轮到 P1-3/4/5（种群、证据包、生命周期——L2 跃迁主体）。

---

## 六、effective-L5 首次对照实验（2026-09-17，零新代码）

> 论文的分级判据：structural L5 = 改进机制被修订且修订被继承使用（本项目已证，
> 见 §5.1 十行审计）；effective L5 = **同等预算 + 独立评测**下，修订后的机制
> 产出/选出更好的后继。本节用既有仪器（events 双 id 谱系、单调计时、锚
> 冻结评测、T0 基线、eval worktree）跑首次受控对照，并固化可复跑协议。

### 6.1 设计

机制修订构成自然实验边界，把 dogfood 全史切成三个纪元：

| 纪元 | 轮次 | 账本事件 | 机制状态 |
|---|---|---|---|
| A 测量残缺 | 1–11（含 1 条 dogfood 前探针） | #0–12 | 级联在宿主残废 PATH 下量宿主不量仓（#22 前）；门从未真实测量（基线永不落盘）；无锚无计时 |
| B 机制修复 | 12–17 | #13–24 | env 规范化→分块→种群齐平→基线 0.9997 落盘→锚装机（首产）→计时引入→stale 守卫 |
| C 测量完整 | 18–22 | #25–30 | 遥测三轮诚实化（#29/#30/#31）→canonical id（#32）→噪声免疫+eval worktree（#33） |

「修订有效」的可操作判据（三选一，且不得牺牲稳定性）：

- **(a) 解锁性真阳性**：修订后出现旧机制**结构上不可能**的真拦截；
- **(b) 效率**：ms/gain 改善超出测量噪声；
- **(c) 稳定性**：拦截/测量能力增强的同时，锚与 T0 读数不降。

### 6.2 结果（账本实测）

| 量 | A | B | C |
|---|---|---|---|
| 事件（成功/失败） | 13（11/2） | 12（7/5） | 6（5/1） |
| 接受率 | 85% | **58%** | 83% |
| 拒绝性质 | 2/2 真阳性（mypy 平台债 #3） | 5/5 真阳性（#19 谱系×2、#21 通道、#27 资产×2） | 1/1 真阳性（#33 锁隔离） |
| 锚触发 | 0（不存在） | 1（装机后首产 8/8） | 4（全 8/8） |
| 计时事件 | 0 | 1 | 6（全单调钟） |
| 门能力 | 不可测量 | 基线 0.9997 落盘、种群齐平 | 噪声免疫、干净 worktree、canonical id |
| 独立评测 | 无 | 锚（1 产） | 锚（4 产）+ T0 持平 |

三点解读：

1. **B 的接受率谷底是验证者在干更多活**：5 次拒绝全部为真（每次都入了
   DEBUG 册），不是机制变差——修复后的 Verifier 首次具备了拒绝能力。
2. **改进瀑布（链式解锁）**：#22 env 规范化→#25 门分块→#26 种群齐平→
   基线落盘→计时引入→#31 睡眠污染暴露→单调钟；eval worktree 启用→
   #33 锁隔离首跑即逮。**每一次机制修订都解锁了上一纪元结构上不可能的
   下一个真缺陷发现**——这是 effective L5 的链式定性证据。
3. **最尖锐单点**：round-22 新启用的 eval worktree 干净门**首次运行即
   逮住** live-tree 时代结构性失明的测试隔离缺陷（#33）——判据 (a) 的
   直接满足。

### 6.3 诚实判定

- **structural L5：成立**（十行机制审计、修订被继承并支配后续轮）。
- **effective L5：方向性阳性，统计上未证。**判据 (a) 已满足（C 纪元
  解锁性真阳性 ≥1）；判据 (c) 成立（锚 4×8/8、T0 持平 0.9997）；判据
  (b) 样本不足（C 仅 6 事件，ms/gain 仍带 18.3M 睡眠离群值）。且存在
  无法剥离的混淆：单宿主、单仓、宿主与引擎同时学习。
- **措辞纪律：宣称「仪器与首次读数已就位」，不宣称等级。**这与论文对
  全领域「structural 有、effective 空白」的判断在同构尺度上重演——
  区别在于本项目现在有可复跑协议。

### 6.4 复跑协议（每 ~10 轮或新机制修订入账时）

1. 从 `events.jsonl` 重算纪元表（切分边界=机制修订落地事件的时间戳）；
2. 新纪元入表，判据 (a)/(b)/(c) 逐项核对；
3. 累积 ≥3 次跨纪元稳定的「有效证据」后，方可把 §2.2 的 L5 行从
   「effective 仍未测」升级为「初步阳性」——升级本身是文档变更，
   走人审。

### 6.5 附带发现（下轮候选）

- **失败事件不带 `eval_workspace` 元数据**（round-19 只给失败路径挂了
  timing）——round-22 的被拒事件因此无法证明它跑在干净树上。观测
  盲点，修法：`_failure_event` 同样附 `eval_meta`。
- 账本早期事件（#0–12）无 landed_gene_ids（旧 schema），跨 A 纪元的
  基因级对比不可做——已知局限，不追溯改写。
