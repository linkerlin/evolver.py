# 演进方案

> 2026-09-24。现行章程。
> 按轮回执在 [`CHANGELOG.md`](CHANGELOG.md)（至 round-78）。
> 论文对照与 effective-L5 实验在 [`RSI演进对照.md`](RSI演进对照.md)。
> 2026-09-01 的差距审计在 [`演进方案_wikiskill对照版.md`](演进方案_wikiskill对照版.md)，仅供考古。
> 后两份的文首状态已过时，以本文件与 CHANGELOG 为准。

## 1. 判断

宿主经 MCP 充当执行器，这条路是对的，而且已经做成。HITL/HOTL 是真门，谱系记落地基因，降级反馈能把下一轮扳回 repair，运行态不进产品 git，验收门停在 shadow，锚在仓外、环内只读。版本钉在 **1.112.0**，挡住了按切片发 minor。

环在转，转的对象几乎总是引擎自己。round-42 之后落地的基因，多数在修测量、卫生和测试针：Hub 粘性 404、冻结 ID 把整块计零、诊断账本、能力轨迹、把上一轮契约钉进测试。RSI 的 P0 与 P1（锚、遥测、基因生命周期、证据包、K=2 种群、机械提案）已经在树上。effective-L5 的复跑停在「方向性阳性、统计上未证」，后面几个纪元没有新的捕获。机制加上去了，后继是否更好，还没有证据。

下一阶段的目标是：一次被接受的变更，改进环外面的任务。验收门继续当安全带。健康安静期里拦截率为零，`ready` 要等人工登记真阳性，这条不能再当路线图。

规模是同一件事的另一面。约 350 个 Python 模块，`cli.py` 两千六百行。Proxy、ATP、WebUI、五套 IDE 钩子、工作流、validator 都在库里。进化环用得上的是其中一小条。宽表面会在 dogfood 里变成下一轮的维修对象。

## 2. 守住

- 引擎不自建 LLM 调度。宿主是执行器。
- `EVOLVER_ACCEPTANCE_SHADOW` 保持打开。转正只由人决定。
- 锚纪元只由人写，环内只读。验证面变异必须过锚。
- HITL 未知值与损坏状态 fail-closed。`AUTO_HIJACK=1` 强制开门，并拒绝宿主自批、自恢复。
- HOTL 的 pause / veto 包住 CLI 与 `--loop`。
- 产品仓不提交 `memory/` 运行态。进化目录用仓外 soak 根。
- 本阶段版本保持 **1.112.0**。阶段结束由人切一次 minor，把 Unreleased 收成一份说明。
- 宿主上报的 `primary_score` 来自级联或门的结果。

## 3. 本阶段：外部适应度

默认路径收成「证据 → 提案 → 级联与锚 → 固化」。用一个环外任务包当适应度。引擎套件保持绿色是约束，包的分数才是得分。

完成判据，满足其一即收阶段：

1. 在连续的外部适应度周期里，至少一次被接受的 diff 不落在 `acceptance/`、`ops/meta_report.py`、`ops/charter_check.py`、`ops/capability_trace.py` 和测试针上；外部任务包分数不降，锚全绿。
2. 约十个这样的周期之后，被接受的变更仍然全是仪器向的。那时把产品说法收窄为「受治理的仓库自维护」，并关掉从未赢过的臂（种群若无胜者；仍默认关闭的 bandit、niche、ATP bridge）。

## 4. 顺序

1. **重复失败走提案。** Evidence Pack 判定该信号族已固化未消、或已接受路径仍失败时，instrument 要求 `swarm_propose`。自由编辑留给新颖信号和结构性改动。固化事件记录级联分。
2. **把 `evolver.bench` 的一个冻结任务包接进周期。** 包分下降则拒绝；持平或上升，且级联与锚通过，则接受。评分规则放在仓外或锚侧，环内不得改。
3. **用第 3 节收口。** 出现环外增益，就切 minor，写清环能被信任去做什么。没有增益，就删臂、改说法。两种结局都保持 shadow。

## 5. 不做

- RSI P2-6 自博弈出题、P2-8 继承工件视图、P2-9 多节点共享进化素材。
- validator 安全模型重写、ATP 商业闭环、PyPI。
- 把拆 `cli.py` 当作独立项目。
- 把 soak `ready` 或首个 verified true positive 当作本阶段出口。真阳性出现时照旧由人登记。
- 按轮次把回执写回本文件。记账只进 CHANGELOG。
- 打开 `enable_event_history`、bandit、niche、novelty gate、operator bandit。有外部分数之后再决定要不要抽样。

## 6. 文档职责

| 文件 | 职责 |
|---|---|
| 本文件 | 现在做什么、何时停 |
| [`TODO.md`](TODO.md) | 可执行清单，按第 4 节改 |
| [`CHANGELOG.md`](CHANGELOG.md) | 按 round 记账 |
| [`RSI演进对照.md`](RSI演进对照.md) | 论文对照与 effective-L5 记录。文首已改为史料；§五里的旧排序不要执行 |
| [`演进方案_wikiskill对照版.md`](演进方案_wikiskill对照版.md) | 2026-09-01 审计。适应度级联现已默认开启 |
