# 演进方案 — 蜂群闭环稳定化（v1.112）

> 2026-09-05。依据对 v1.98–v1.111 一周改动的系统性审阅。
> 与 [`演进方案_wikiskill对照版.md`](演进方案_wikiskill对照版.md) 的关系：后者仍是 Sprint 26–30 / Node 对标的长期差距清单；**本文件是蜂群栈落地后的下一阶段章程**——先稳互锁，再谈 soak 转正与继续收割。
> 工作清单见 [`TODO.md`](TODO.md)。

## 1. 结论

把宿主 Agent 经 MCP stdio 收成执行器（引擎不自建 LLM 调度）是对的。五轮 dogfood 也是真的：级联被 PATH 跳过、运行态混进变异提交、distill 静默零产出、已落地基因反复重派、`complete_agent` 无视失败——这些都进了测试。

不宜据此认为蜂群已经可以无人值守进化 `master`。

- HITL/HOTL 是包装，不是互锁。
- 反馈 E 进了日志和提示词，没有改选择器。
- 固化提交与冷却窗口记的是**剧本基因**，不是落地基因。
- 验收门诚实停在 `collecting`（gated_runs=4 / 需 ≥20）。
- 一天 13 个 minor 是把 changelog 当日记。

**本阶段唯一目标：冻结表面，把门、否决、反馈、谱系做成代码路径上的事实，把运行态踢出产品 git，同一版本把 gated_runs 从 4 跑到 20。** 不再按「一个收割切片一个 minor」发版。不把 `EVOLVER_ACCEPTANCE_SHADOW=0`。

## 2. 一周事实（压缩）

| 弧线 | 版本 | 实际产物 |
|---|---|---|
| 蜂群接管 | 1.98–1.106 | tick→执行→distill→solidify→feedback；HITL/HOTL/Hooks/技能桥/自适应权重 |
| Dogfood | 1.107–1.111 | soak 报告、YAML 工作流、五轮真实变异、冷却、distill hint |
| 卫生 | 1.108 + 五次 chore | `_is_runtime_state` 过滤变异提交，但 `memory/` 仍被手动入库 |

Dogfood 的真实图像：宿主在仍在响的 `f001 hub_offline` 上反复选 Hub 剧本，改的却是引擎 UX/选择器，再给自己打 `primary_score=1.0`。闭环存在；选择、归因、评估三臂都松。

## 3. 决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 发布节奏 | 本阶段一个 minor：**1.112.0** | 行为变化（fail-closed、pause 进周期、机械 repair）不是补丁 |
| HITL 默认 | `HITL_MODE` 仍默认 `off`；`AUTO_HIJACK=1` 时强制 on | 交互宿主不突然阻塞；无人值守必须有门 |
| skip 验证 | 无 pending run 即拒绝；审批下沉进 `solidify()` | 消灭 `solidify_skip_validation:unknown` 粘性键 |
| MCP 转达人类 | `AUTO_HIJACK=1` 时拒绝 MCP 的 approve / resume / unveto | 同一宿主不能自批自恢复；交互模式仍可转达 |
| HOTL 范围 | pause/veto 进入 `_run_single_cycle`，不只 MCP 包装 | 文档写的「纵深防御」必须对 CLI/`--loop` 也成立 |
| 损坏的状态文件 | fail-closed（HITL 视为拒绝；监督视为暂停） | 静默清空等于解除保险 |
| 降级反馈 | `swarm_feedback:degraded` → `force_category=repair` | 与 autopoiesis 摩擦同路；提示词权重只作附加 |
| 基因谱系 | 事件同时记 `gene_id`（剧本）与 `landed_gene_id`（蒸馏） | 提交说明与冷却罚落地基因 |
| 运行态 | gitignore `memory/` 运行文件与 `evolver/.config/`；保留 `LESSONS_LEARNED.md` | 产品仓不是 dogfood 节点 |
| `enable_event_history` | **本阶段不打开** | 冷却已独立加载事件尾；打开会改变 saturation/dedup，留给 soak |
| 验收门转正 | 不转 | 4/20；报告只回答「数据是否支持」 |
| 继续收割 EvoX / 扩 CLI | 本阶段不做 | 先稳 |

## 4. 停止做的事

- 不要再按收割切片 bump minor。
- 不要再 `chore: runtime state sync`。
- 不要在产品仓 `master` 上继续 `evolver: gene_*` 直推。
- 不要扩 `cli.py` / `mcp_server.py` 三处齐写（下一只工具先拆 `swarm/` 包）。
- 不要在 soak 未完成时增加 `EVOLVER_*` 旋钮。
- 不要把 Acceptance Gate「~85%」读成可以 `SHADOW=0`。

## 5. 稳定化设计

### 5.1 HITL 成为真门

现状：`skip_validation=True` 只在 `swarm_solidify` 外包一层；默认 off 自动批准；`run_id` 缺失时 subject 为 `…:unknown`；`HITL_MODE=ON` 不等于 on；状态文件损坏返回空列表（旧拒绝蒸发）。

目标：

1. `parse_hitl_mode`：`on/true/1/yes` → on；`off/false/0/no` → off；未知 → **on**（fail-closed）。
2. `SWARM_AUTO_HIJACK=1` ⇒ `hitl_mode_enabled()` 为真。
3. `solidify(skip_validation=True)` 自身过 HITL；无 `run_id` 则 `skip_validation_requires_pending_run`。
4. 状态文件加锁；读失败 → 拒绝（不新开申请）。
5. `list_pending` 惰性过期 TTL。

不在本阶段做：把 MCP `approval_resolve` 改成密钥协议（交互宿主仍需转达）。仅在 AUTO_HIJACK 下切断。

### 5.2 HOTL 包整台引擎

现状：tick **先跑完整周期**（`print(prompt)` 进 stdout），再清空返回字段；`engine_log` 仍截提示词尾。Solidify veto 只匹配 `solidify:{run_id}`。pause 不挡 CLI/`--loop`。监督 JSON 损坏回到 `running`。

目标：

1. `_run_single_cycle` 开头 `auto_pause_check()` + `is_paused()` → 跳过周期。
2. `select_phase` 之后、`dispatch_phase` 之前按基因 id/name/run_id 检查 veto；命中则不 dispatch（无 prompt、无 solidify 状态、无 `engine_log` 泄漏）。
3. `swarm_solidify` / `solidify` 路径按基因 id 再挡。
4. `swarm_tick` 取实例锁；冲突 → `instance_lock_held` / `stop_and_report`。
5. 监督状态损坏 → 视为 `paused`（fail-closed）。绊线跳过坏行，不 `break` 清零连击。
6. 过短/过泛 veto 模式拒绝（`< 4` 字符，或 `gene_` / `run_`）。
7. CLI `supervise veto --note` 真正传入。
8. MCP：`approval_resolve` / `supervise` / `workflow_act` 标 destructive；AUTO_HIJACK 下拒绝 approve/resume/unveto。
9. `evolver://instrument-prompt` 只渲染文本，不 `swarm_boot`（无 mailbox hello）。

### 5.3 反馈变成机械量

现状：`swarm_feedback:degraded` 写入 pending_signals；`_choose_category` 不认它；自适应权重只进 GEP 提示词。宿主自报 1.0 会把自适应推向 novelty。

目标：`select_phase` 若信号含 `swarm_feedback:degraded`，或自适应 verdict 为 `repair_bias`，则设 `autopoiesis_repair_bias` 并 `force_category="repair"`（与 preflight 摩擦同路）。管线测试：三连降级 → 下次 mutation 类别为 repair。

### 5.4 基因谱系

现状：`_commit_mutation` 用 `selected_gene_id`。round-4 的 format-hint 提交写成 `gene_hub_fetch_resilience`。冷却罚错对象。蒸馏基因活在 gitignore 的 `.evolver/`。

目标：

1. `swarm_distill` 把安装成功的 gene id 写入 solidify state 的 `landed_gene_ids`。
2. EvolutionEvent.mutation 同时带 `gene_id`（剧本）与 `landed_gene_id` / `landed_gene_ids`。
3. 提交说明优先落地基因。
4. 冷却窗口对两类 id 都惩罚。

本阶段不把 dogfood 蒸馏基因提升进 `genes.seed.json`（那是 soak 之后的基因组政策）。

### 5.5 工作流门可审计

现状：`default_cascade_runner` 只留 stderr 尾；ruff/pytest 诊断在 stdout。round-5 WAL 里 ruff rc=1、`stderr_tail=""`。无 `cwd`。嵌套 `agent`/`approval` 忽略暂停返回值。`repair.yaml` 谎称批准后引擎会 distill+solidify。

目标：合并 stdout+stderr 尾；`cwd=get_workspace_root()`；尊重 `timeout_ms`；嵌套步骤传播 park；模板写真话。

### 5.6 仓库卫生与发布

- gitignore：`memory/evolution/*.json`、`*.jsonl`、子目录（含 wiki/workflows/哈希仓）、`memory/*.jsonl`、`evolver/.config/`。**保留** `memory/evolution/LESSONS_LEARNED.md`。
- `git rm --cached` 已跟踪运行态。工作副本保留。
- CHANGELOG 只留一个 `## [Unreleased]`；历史 Sprint 8–10 块改标 `[archived]`。
- `scripts/check_changelog.py` 拒绝多个 Unreleased 标题。
- 包版本 **1.112.0**。
- `EVOLVER_SKILL_ROOTS` 按 `os.pathsep` 分割（与文档一致）。
- CLI distill 零资产时给出与 swarm 相同的 hint。

### 5.7 明确不做（本阶段）

| 项 | 原因 |
|---|---|
| S26.5 干净 worktree 跑门 | 下一阶段；本阶段先把现有门记全、停污染 git |
| `enable_event_history` 转默认 | 冷却已不依赖它；打开会改变信号历史语义 |
| 验收门 / fitness 转执法 | soak 样本不够 |
| Validator 安全模型、ATP 商业 E2E | 弱面，但不阻塞互锁修复 |
| 拆 `swarm/` 包、瘦 `cli.py` | 稳定化之后 |
| PyPI / mkdocs（S30.3） | soak 之后 |
| 把 MCP 审批改成带外密钥 | AUTO_HIJACK 切断足够；完整协议另开 |

## 6. 验收

代码（本阶段结束必须全绿）：

- HITL：未知 mode 当 on；损坏 store 拒绝 skip；无 pending run 不能 skip；`ON`/`true`/`1` 都能打开。
- HOTL：pause 后 `evolver run` / `_run_single_cycle` 不 dispatch；veto 基因 id 后无 `dispatch_prompt` 且 `engine_log` 不含 `BUILT_PROMPT`；solidify 按基因 id 拦截。
- 反馈：pending `swarm_feedback:degraded` → `select_phase` 的 mutation.category == `repair`。
- 谱系：distill 后 solidify 事件含 `landed_gene_id`；冷却命中落地 id。
- Gate：只写 stdout 的失败命令，WAL/`stderr_tail` 能读到诊断。
- Changelog 检查：恰好一个 Unreleased。
- `git ls-files memory` 只剩 `LESSONS_LEARNED.md`（及本方案明确保留者）。

运行（稳定化之后，不在本提交宣称完成）：

- 同一版本、外置 `EVOLUTION_DIR`、不向产品仓提交运行态，把 gated_runs 做到 20。
- 再由人类决定是否 `EVOLVER_ACCEPTANCE_SHADOW=0`。

## 7. 之后（稳完才做）

沿用演进方案 §8 移交余项的顺序：

1. S26.5 验收门在干净 worktree 跑。
2. S30.4/30.5 环境变量分组与 flag 退役（252 → 目标 ≤80）。
3. 机械提案（S29）作为宿主默认路径，替代自由编辑 + distill。
4. Validator 安全模型（仍约 50%）。
5. S30.3 发布决策。

## 8. 实施顺序

见 TODO.md。依赖：卫生与版本号可并行于互锁；HITL/HOTL/反馈必须先于下一轮 dogfood。

## 9. 审阅回执（2026-09-05，v1.112.0 落地后）

> 本文件原名「研究方案.md」，经审定为现行阶段章程后改名「演进方案.md」；
> CHANGELOG/TODO/README.zh 之引用与 wikiskill 对照版头部歧义已同步消除。

### 9.1 诊断核实（§1/§2 之断言逐条对码验证）

| 断言 | 核实 | 证据 |
|---|---|---|
| HITL/HOTL 是包装非互锁 | ✅ 属实 | 旧 `swarm_tick` 才查 pause；veto 在周期跑完后仅清返回字段（solidify 状态已被 dispatch 写下）；`HITL_MODE=="on"` 大小写敏感（`ON`/`true`/`1` 皆 off） |
| 反馈 E 未改选择器 | ✅ 属实 | `swarm_feedback:degraded` 只进 pending_signals；`_choose_category` 不识；自适应权重仅入提示词 |
| 提交记剧本基因非落地基因 | ✅ 属实（git 实证） | round-4 引擎提交 `d68a7fc` 报 `gene_hub_fetch_resilience`，实际蒸馏落地基因为 `gene_distill_format_hint` |
| 宿主自报 1.0 推向 novelty | ✅ 属实 | round-3/4/5 feedback 皆 1.0；结构性自评自利 |
| 运行态仍手动入库 | ✅ 属实 | 五次 `chore: runtime state sync` 提交 |

### 9.2 实现忠实度（§5 → 代码逐节核对）

- **5.1 HITL**：`parse_hitl_mode`（`config.py:64`，未知→on）；`AUTO_HIJACK` 强制开门
  （`hitl.py:hitl_mode_enabled`）；状态文件加锁 + 损坏 `HitlStoreError` fail-closed；
  `solidify()` 自身过门（`solidify.py:817`）；无 pending run 拒绝 skip
  （`solidify.py:814`，消灭 `…:unknown` 粘性键）。✅
- **5.2 HOTL**：`_run_single_cycle` 开头 pause 检查（`runner.py:104-108`，CLI/`--loop`
  同享）；select 后 dispatch 前 veto 早退（`runner.py:135-142`——连 solidify 状态都
  不写，优于只扣提示词）；solidify 按基因 id 再挡（`solidify.py:802-809`）；
  监督损坏→paused（`_FAIL_CLOSED_BY="auto:corrupt_store"`）；`swarm_tick` 实例锁
  （`swarm.py:345-349`）；MCP `host_relay_blocked` 拒绝 AUTO_HIJACK 下转达
  approve/resume/unveto。✅
- **5.3 反馈**：`select.py:56-73`——`swarm_feedback:degraded` 或自适应 `repair_bias`
  → `autopoiesis_repair_bias` + `force_category="repair"`。✅
- **5.4 谱系**：`record_landed_gene_ids`；事件同时携带 `gene_id` 与
  `landed_gene_id(s)`（`solidify.py:113-116`）；冷却对双类 id 惩罚
  （`selector._ids`）。✅
- **5.5 工作流门**：stdout+stderr 双尾（各 2000 字符）、`cwd=workspace`、尊重
  `timeout_ms`；嵌套 `agent`/`approval` 传播 park（`workflow.py:464/473`）；两模板
  已写真话（批准后宿主须自调 `swarm_distill`+`swarm_solidify`）。✅
- **5.6 卫生**：gitignore 覆盖运行态（check-ignore 3/3 命中）；
  `git ls-files memory` 仅剩 `LESSONS_LEARNED.md`；单一 Unreleased +
  `check_changelog.py` 通过；Sprint 0-10 历史块已 `[archived]`；
  `EVOLVER_SKILL_ROOTS` 改 `os.pathsep`。✅

### 9.3 §6 验收执行记录（2026-09-05）

- 代码门：`ruff check` ✅ / `ruff format --check`（622 文件）✅ / `mypy src` strict
  0 错误（319 文件）✅ / `pytest -m "not llm"` **3492 passed** ✅
  （v1.112 新增互锁测试分布于 test_hitl +3 / test_supervision +5 / test_swarm /
  test_workflow / selector_cooldown / recent_api_surface）。
- 运行门（gated_runs 4→20、外置 EVOLUTION_DIR、不向产品仓提运行态）：**未宣称
  完成**，留待下一轮 dogfood——与 §6「不在本提交宣称完成」一致。

### 9.4 审阅结论与遗留

1. **方案诊断全部属实，实现全部落点，无发现偷工**。fail-closed 方向贯穿
   （mode 未知→on、损坏→拒绝/暂停、AUTO_HIJACK→断转达）。
2. 此前 `complete_agent` 失败契约修复（`ok:False`→`agent_failed`）在 v1.112
   重构中存活并保留语义。✅
3. 遗留小项（不阻塞，记入下次）：
   - DEBUG.md 经验簿应补 v1.112 互锁硬化条目（自批自恢、归因错位两类）；
   - `parse_hitl_mode` 未知值→on 是行为破坏性变更（如用户曾设
     `EVOLVER_HITL_MODE=disabled`），CHANGELOG 已述，升级指南可补一句；
   - 谱系双 id 落地后，round-1~5 的历史事件仍只有剧本 id——冷却对旧事件
     天然失效一个窗口期，属预期成本。

### 9.5 回执跟进（同日）

| 遗留 | 处置 |
|---|---|
| DEBUG.md 自批自恢 / 归因错位 | 已补条目 #12、#13；#9 版本改为 v1.112.0 |
| HITL 未知值破坏性变更 | CHANGELOG 1.112.0 增 Upgrade notes；`disabled` 测例钉住 fail-closed |
| 旧事件无 landed_gene_id | 记入 #13 经验：一个冷却窗口的预期成本，不回填 |
| `演进方案.md` 曾被 gitignore | 已移出 ignore，使 README.zh / wikiskill 对照版链接可入库 |

## 10. 下一阶段：soak 运行门（不在 1.112 宣称完成）

目标：同一包版本、**外置**进化目录，把 `gate-report` 的 `gated_runs` 从 4 做到 ≥20，
再由人决定是否 `EVOLVER_ACCEPTANCE_SHADOW=0`。

```bash
uv run evolver soak setup                 # $EVOLVER_HOME/evolver.py-soak/{evolution,gep} + env.sh
eval "$(uv run evolver soak exports)"     # 把 EVOLUTION_DIR / GEP_ASSETS_DIR 指到仓外
export EVOLVER_FF_ENABLE_EVAL_WORKTREE=1  # S26.5：级联/验收门在干净 worktree 跑

# 交互宿主：不要开 AUTO_HIJACK。无人值守才 EVOLVER_SWARM_AUTO_HIJACK=1
# （此时 HITL 强制 on，skip_validation 须 evolver hitl approve）

uv run evolver mcp
uv run evolver soak status --json         # inside_repo=true 说明还在写产品仓
uv run evolver gate-report --json         # 仓内 EVOLUTION_DIR 会警告
```

纪律：

- 不要 `git add memory/` 或 `chore: runtime state sync`。
- 不要 bump minor。soak 用 1.112.0。
- 宿主 `swarm_feedback` 必须反映级联真分，禁止例行 `primary_score=1.0`。
- `f001 hub_offline` 仍在活记忆里时，不要指望 Hub 剧本自己让位——冷却只罚近窗。

S26.5 评估隔离已落地为 flag（默认关）：`EVOLVER_FF_ENABLE_EVAL_WORKTREE=1` 时
级联与验收门在干净 worktree 跑，失败回退 live cwd。soak 请打开此 flag。

### 结局（2026-09-17 回填）

gated_runs 于 round-21 达 **20/20**（2026-09-04 → 09-16 窗口）。判定：
`verdict=false_kill_high`——**数据不支持转正，SHADOW 保持 on，转正仍留人类**。

两次伪杀（round-29 live 读数，非写的那「唯一次」）：

| 序 | 轮 | 事件 | Δ | 性质 |
|---|---|---|---|---|
| 1 | 21 | `gene_gep_repair_from_errors` | −0.0567（repeat0 0.8863 / repeat1 0.9997） | 分块超时 → round-22 已校准（chunk retry） |
| 2 | 24 | `gene_load_threshold_cpu_aware` | −0.00014（repeat0 0.99943 / repeat1 0.99972） | 1/3519 单测试抖动 → round-25 已改 ANY-mismatch 仲裁（#36），但**基线仍是单观测**（§11.4 P0-2） |

round-29 `gate-report --json` 实况：gated_runs **20**（窗口封顶）、累积 **28**、
shadow_rejected **2**、interception **0.10**、disagreements **2**、
false_kill_risk **1.0**、`inside_repo=true`。§10 这段原本回填的
「1 / 0.05 / 1」已于 round-24 之后过期——**回填式回执随数据集漂移，本
文档自己也未能免俗**（治法见 §11.4 P1）。

诚实注记（处方债，重攒样本前须补）：

1. 本段 soak 实际在**仓内**进行（`inside_repo=true`；外置
   `evolver.py-soak` 工作区从未建立）。缓解成立——运行态全程 gitignore、
   零提交——但「外置进化目录」这行处方**仍未照办**（round-29 依旧 true）；
2. `EVOLVER_FF_ENABLE_EVAL_WORKTREE` 曾长期未开。**已自行好转**：round-22
   起连续 **8 轮**（round-22~29，另含 round-28 一次未挂门结果的失败事件）
   事件均带 `eval_workspace.isolated=true`，干净 worktree 门真实运转过。
   旧注记「从未运行过」作废；剩余问题是 flag 仍默认关，靠会话自觉。

守住的部分：版本钉 1.112.0、零新 env 旋钮、SHADOW 未动、运行态零提交、
RSI P0 两项（锚定评测 + meta-report）以纯测量形式在 soak 期内落地——详见
`RSI演进对照.md` §五 落地实况审计。

---

## 11. 章程实况复核（round-29，2026-09-17）

> 触发：round-29 Mimosa 深扫 triage（`docs/mimosa-triage.md`）之后，对本章
> 程做一次「条文 vs live」的对码复核。方法：读 `.evolver/gep/events.jsonl`
> 与 `gate-report --json`，不采信 §9/§10 的既往回填。**本章带时点戳，下一
> 次 dogfood 若再产生 20+ 样本，须重新核对。**

### 11.1 实现忠诚度复查（round-13 → 29）

| 条 | 复核 | 证据 |
|---|---|---|
| 5.1 HITL 真门 | ✅ 存活 | `parse_hitl_mode` 未知→on；`HitlStoreError` fail-closed；`hitl_mode_enabled` 含 AUTO_HIJACK |
| 5.2 HOTL 包引擎 | ✅ 存活 | `runner.py:104` `auto_pause_check`+`is_paused` 早退；select 后 `check_veto`→`dispatch_prompt=""` |
| 5.3 反馈机械 repair | ✅ 存活 | `select.py:56-73` degraded/repair_bias → `force_category="repair"` |
| 5.4 基因谱系 | ✅ 存活 | `solidify.record_landed_gene_ids` + 事件双 id |
| 5.5 工作流门 | ✅ 存活 | stdout+stderr 双尾、`cwd=get_workspace_root()`、`timeout_ms` 生效 |
| 5.6 仓库卫生 | ✅ 存活 | `git ls-files memory` = 仅 LESSONS_LEARNED.md；`check_changelog.py` OK |
| 纪律：版本/旋钮/运行态 | ✅ 守住 29 轮 | 1.112.0 未动；round-22~29 零新 `EVOLVER_*`；运行态零提交 |

纪律的副作用一条：round-22~29 共八轮、约 17 条 DEBUG 语义变更在 CHANGELOG
中几无痕迹——「不 bump minor」换来的是**外部不可辨识**。解法不是发版，是
把 round 记账写进 Unreleased（§11.4 P1）。

另注一条自查：本复核初稿自身的三个 live 计数曾各偏一（锚触发 8 记成 9、
隔离覆盖 8/9 记成 9/9、把 round-24 的**当时**状态当成**现存**缺陷）。成因
是人工验证方法不当（全文子串匹配结构字段）。这条与其说是错误，不如说是
§11.2 第二项论点的自证。

### 11.2 三处要害

| # | 要害 | 实证 | 性质 |
|---|---|---|---|
| 1 | **主指标与目的脱钩且已饱和** | `report.py:51` 窗口切片，`gated_runs` 天花板 = `GATE_SOAK_MIN_RUNS` = 20；累积 28 只报 20。20/20 达标当天 verdict 却是 `false_kill_high` | 达成指标 ≠ 达成目的；且此后**读数恒定**，说不出「距带内还差几样本」 |
| 2 | **回执是人工快照，随时点漂移** | §10 回填的三行数字先后两次过期（1/0.05/1 → 2/0.10/2）；§5 无处_MACHINE 的复核面 | 真相源在人头里，不在机器里 |
| 3 | **章程只治行为互锁，不治数据入口** | round-29 的 `llm_template` shell 注入面不在 `ANCHOR_TRIGGER_SURFACES`、不在 `META_MECHANISM_SURFACES`、§5/§7 全无条目；靠外部扫描器偶然发现 | 地图级盲区，非执行不力 |

加重证据（复核中新发现）：按现行 `gate_soak_recommendation` 分支，若两笔
伪杀滑出且窗内再无拒绝，则 `shadow_rejected=0 → interception 0 < 0.05 →
verdict=under_intercepting`——**结构性永远到不了 `ready`**。健康安静期的终
态不是转正建议，而是另一个非 ready 判定。故「≥1 次人工确认真阳性」不是
锦上添花，是解锁键。

### 11.3 §7「之后」清单的当前状态

| §7 项 | 状态 |
|---|---|
| S26.5 干净 worktree 跑门 | **已实施未默认**（round-22 起连跑 8 轮，flag 默认关），见 §10 注记 2 |
| S30.4/30.5 env/flag 退役 | 未动（`EVOLVER_*` 唯一值 218 个） |
| S29 机械提案作宿主默认路径 | 未动 |
| Validator 安全模型 | 未动（仍约 50%） |
| S30.3 发布决策 | 未动 |

阶段推定：**v1.112 稳定化已在 round-21 达成其自设目标，但阶段从未宣布结束**，
导致「之后」清单从未正式启动，而 dogfood 继续在旧章程下跑了八轮。缺的不是
目标，是**阶段切换仪式**。

### 11.4 收口单（本轮起执行）

| 优先级 | 项 | 内容 |
|---|---|---|
| **P0-1** | 退出判据重写 | metrics 增 `gated_cumulative`；判据改为 `false_kill_risk≤0.1` ∧ **≥1 次人工确认真阳性** ∧ 末次校准变更后窗内零未解释分歧；`shadow_rejected=0` 时判 `collecting_verified` 而非 `under_intercepting`（解死锁）。触 `acceptance/` 面 → 同轮升锚纪元 |
| **P0-2** | T0 双侧重复 | `orchestrator.py:221` 基线由单浮点合成单条观测 → 基线同样重复测量并走同一仲裁（顺带堵「侥幸低基线→假放行」）。**次序：双侧重复 > 失败用例集合判定 > ε 死区**——ε 会盲掉确定性套件上真实的单测试回归 |
| P1 | `charter-check` 机器化回执 | 定期输出版本/round/gated_all/漂移对比/env 数/运行态 git 计数/锚纪元；并把「锚是否覆盖 acceptance/ 变更」从人头纪律变成机器断言 |
| P1 | TODO.md 收口 + CHANGELOG 按 round 记账 | 本文档 §11 之前的部分不再单方面篡改，TODO 与 unilateral reality 对齐 |
| P1 | worktree 默认开 | 方向同意，但**先审回退路径**（静默回退 live cwd 则默认开名不副实）；且与「一阶段一 minor」有张力，须由章程先给规则 |
| P2 | 数据入口清单 | 盘点「半信任文本入执行面」；自由文本占位符按**占位符身份裸用即拒**（黑名单问题整体消解）；`enable_llm_template` 的开启以「入口面已注册锚」为前置互锁 |

执行顺序按 P0-1 → P0-2：假杀已入窗，前向校准做得再好也改不了当前 verdict；
先让 switch 具备可达性，再做未来卫生。

### 11.5 round-30 实施记录（P0-1 已落地，2026-09-17）

| 落点 | 改动 | 验证 |
|---|---|---|
| `acceptance/report.py` | `gated_cumulative` + `verified_*` 计数（仍为纯函数、零 I/O） | mypy 324 文件 0 错误 |
| `acceptance/report.py` | 判据重排：`unverified` / `collecting_verified`，撤回不可达的 `under_intercepting` | 28 passed |
| `ops/soak_env.py` + `cli.py` | 仓外只读账本 `anchor/gate-verifications.jsonl` 读取与接线 | 5 passed |
| `assets/anchor/case-gate-verdict-reachability` | 三条不变量冻结（饱和可见 / 无指控不给 ready / 确认滑出仍解死锁） | epoch 6，11/11 PASS |
| `DEBUG.md` #41 + CHANGELOG | 四段式入账 + 按 round 记账 | `check_changelog.py` OK |

实跑读数（改后）：gated_runs 20（窗）/ 累积 **28** / shadow_rejected 2 /
interception 0.10 / false_kill_risk 1.0 / `verified_true_positives 0` /
verdict **`false_kill_high`**。

一条记录下来的自我修正：判据初稿把 `unverified` 排在 `false_kill_high` 之前，
结果当前读数从 actionable 的「门过紧」退化成「你没交材料」——**诊断类判定应
优先于完备性判定**。代价是尚无人工登记时 verdict 会在两个"不可转正"之间游走，
这是有意的：先说最该修的那件事。

未动（下一轮）：P1 worktree 默认开（先审回退路径）、P1 仓外运行态强制互锁、P2 数据入口清单。

### 11.6 round-31 实施记录（P0-2 与 P1 charter-check 已落地，2026-09-18）

| 落点 | 改动 | 验证 |
|---|---|---|
| `acceptance/orchestrator.py` | `save_baseline` 支持 `repeats` 与 `adjudication`；`load_baseline_repeats` 反序列化多观测；`run_acceptance_gate` 双侧对称接入（`baseline_cwd` 现场测量 / `baseline_repeats` 历史观测 / scalar fallback 三级阶梯） | 19 passed |
| `acceptance/solidify_hook.py` | 加载并向门控传入 `baseline_repeats`；新基线固化时同步落盘双侧观测与多数值仲裁元数据 | 65 passed |
| `ops/charter_check.py` + `cli.py` | P1 机器化回执落地：版本/round/gated累积/漂移度量/env统计/git运行态卫生/锚纪元全自动核验；接入 CLI `evolver charter-check [--json]` | 4 passed |
| `assets/anchor/case-t0-bilateral-repeats` | 新增第 12 探针，冻结持久化多观测保真、双侧重复不塌缩为单点、旧基线优雅后向兼容三不变量 | **Epoch 7**，12/12 PASS |
| `eval_worktree.py` + `feature_flags.py` | `enable_eval_worktree` 转默认开启（True）；加固回退路径：新增 `check_dirty_runtime` 扫描脏运行态并在 fallback 时向 stderr 明确告警、`eval_meta` 记录 `dirty_runtime`；支持 `EVOLVER_EVAL_WORKTREE_STRICT=1` 严格模式抛出异常 | 7 passed |
| `ops/soak_env.py` + `cli.py` + `runner.py` + `mcp_server.py` | P1 仓外运行态强制互锁（§11.4 #4）：`maybe_route_to_soak` 检测到 `inside_repo=True` 且非测试时自动路由至 `$EVOLVER_HOME/evolver.py-soak`；`setup` 幂等增量搬运历史资产与事件；CLI 与 runner/mcp 双层设防；`charter-check --soak` 机器验证直通 | 19 passed |
| `DEBUG.md` #42 + `TODO.md` | 四段式入账 + TODO 对齐 live reality，P0-2、P1 charter-check、P1 worktree 与 P1 仓外互锁全标完成 | OK |

实跑读数（`evolver charter-check --soak`）：
`Acceptance Gate: verdict=false_kill_high, cumulative=28, rolling=20`
`Interception / Risk: rate=0.10 (met=True), false_kill=1.0 (met=False)`
`Runtime / Worktree: inside_repo=False (outside_met=True), clean_git=True`
`Anchor Suite: installed=True, epoch=7, cases=12, covers_acceptance=True`
`Env Footprint: 180 unique EVOLVER_* (target <= 80, met=False)`
`Promotion Status: BLOCKED (Shadow Mode Maintained)`

未动（下一轮）：P2 数据入口清单（§11.4 #5）、S29 机械提案通道（§11.4 #6）、S30.4/30.5 env 退役。

### 11.7 round-32 实施记录（S29 机械提案 + P2 数据入口 + S30.4/30.5 env 退役全部落地，2026-09-18）

| 落点 | 改动 | 验证 |
|---|---|---|
| `gep/proposal.py` + `gep/solidify.py` + `cli.py` + `swarm.py` + `mcp_server.py` | **S29 机械提案通道**（§11.4 #6）：引入 `GeneProposal`、`ProposalEdit` 强契约数据结构，支持 `exact_match`、`anchor_pattern`、`unified_diff`；`solidify(proposal=...)` 机械应用落地（支持 dict/Path/JSON）；`cli.py solidify --proposal <file>` CLI 参数；`swarm_propose` MCP 工具向蜂群宿主开放 | `tests/gep/test_proposal.py` 14/14 PASS |
| `gep/llm_template.py` + `gep/feature_flags.py` + `ops/charter_check.py` | **P2 数据入口清单与防御**（§11.4 #5）：声明 9 类半信任自由文本占位符（prompt/diagnosis/response/context/content/diff/error/log/logs），在 shell 模板中按**占位符身份裸用即拒**（消解黑名单军备竞赛），审计记录 `<stamp>_refused.txt`；自动文件管道落地（`{<name>_file}` 自动材料化 `<stamp>_<name>.txt`）；`enable_llm_template` 注册锚互锁（未进 `ANCHOR_TRIGGER_SURFACES` 拒绝开启）并在 `charter-check` 中提供机器断言 | `tests/gep/test_llm_template.py` 15/15 PASS |
| `assets/anchor/case-data-ingress-guard` | 升锚 **Epoch 8**：新增第 13 冻结探针，覆盖自由文本身份级裸用拒认、合法文件通道放行、非法注入截断与审计标记落地三大安全不变量 | **Epoch 8**，13/13 PASS |
| `config.py` + `adapters/` + `gep/` + `proxy/` + `webui/` + `atp/` + `ops/` | **S30.4/30.5 env/flag 退役**（§11.4 #7）：系统性梳理并收敛冗余内部环境变量，将网络超时、固化参数、内部默认配置、单例锁目录、废弃探针标记等折叠为稳定常数或已有规范变量（`EVOLVER_SETTINGS_DIR`、`EVOLVER_LOGS_DIR`、`EVOLVER_REPO_ROOT`、`A2A_NODE_ID`），全局独立 `EVOLVER_*` 从 180 骤降至 **77**（≤80，`charter-check` `met=True`） | `charter-check --soak` met=True |
| `TODO.md` + `docs/env-registry.md` | 工作清单 P1 阶段 7 项全部标为 `完成`；环境注册表自动重新生成，记录收敛后各变量机制 | OK |

实跑读数（`evolver charter-check --soak`）：
- Acceptance Gate: `verdict=false_kill_high, cumulative=28, rolling=20`
- Interception / Risk: `rate=0.10 (met=True), false_kill=1.0 (met=False)`
- Runtime / Worktree: `inside_repo=False (outside_met=True), clean_git=True`
- Anchor Suite: `installed=True, epoch=8, cases=13, covers_acceptance=True, covers_ingress=True`
- Env Footprint: `77 unique EVOLVER_* (target <= 80, met=True)`
- Promotion Status: `BLOCKED (Shadow Mode Maintained)`




